<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.wade8.broadcastsreciverlearning">

    <!--<uses-permission android:name="android.permission.BROADCAST_STICKY" />-->
    <uses-permission android:name="com.example.wade8.broadcastreciverlearning.PERMISSION"/>
    <permission android:name="com.example.wade8.broadcastreciverlearning.PERMISSION"/>
    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity"
            android:launchMode="singleInstance">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".Activity2"
            android:launchMode="singleInstance">
        </activity>

        <receiver
            android:name=".MyReceiver"
            >
            <intent-filter>
                <action android:name="com.example.wade8.broadcastreciverlearning.CHANGE_TEXT"/>
            </intent-filter>
        </receiver>

    </application>

</manifest>